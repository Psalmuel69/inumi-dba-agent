# =============================================================================
# LOCAL DEVELOPMENT ONLY — fake, non-routable credentials for the Execution
# Service's mock query executor. Never used when SECRETS_PROVIDER is
# anything other than `local_dev`. Never commit real credentials here.
# =============================================================================

credentials:
  sqlserver-dev-01:
    host: localhost
    port: 1433
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: SampleDev

  sqlserver-uat-01:
    host: localhost
    port: 1434
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: SampleUAT

  corebanking-prd-01:
    host: localhost
    port: 1435
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: CoreBanking

  postgres-dev-01:
    host: localhost
    port: 5433
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: sample_dev

  postgres-uat-01:
    host: localhost
    port: 5434
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: sample_uat

  postgres-prod-cluster-01:
    host: localhost
    port: 5435
    username: dev_readonly
    password: dev-only-not-a-real-secret
    database: analytics_prod
